example.name = Week 15 Practice
example.path = Custom
example.additionalFile.1 = src/practice.java

Practice example for week 15 practice session.

Verify an operation that uses binary search to find a given entry in an array of entries that are in sorted order.
The proof obligation is verifiable without user interaction.

(Modified from an example provided by KeY)
